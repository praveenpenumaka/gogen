# API

{{ .Project.Description }}

## Overview
This server was generated by the [gogen] (https://) project.

## Database migration

* Move .env.sample to .env and update the appropriate values
* make db.reset

### Running the server

To run the server, follow these simple steps:

```
make run-api
```

To run the server in a docker container
```
docker build --network=host -t <base folder> .
```

Once the image is built, just run
```
docker run --rm -it <base folder>
```
